<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Профиль</title>
    </head>
    <body>
        <header>
            <a href="/">
                <img src="/img/logo.png" alt="Logo" style="height: 50px;">
            </a>
        </header>
        <h1>Профиль</h1>
        <p>Логин: <%= user.username %></p>
        <p>Роль: <%= user.role %></p>
        <a href="/auth/logout">Выйти</a>

        <% if (user.role === 'admin') { %>
            <h2>Список пользователей</h2>
            <ul>
                <% users.forEach(u => { %>
                    <li>
                        <%= u.username %> (<%= u.role %>)
                        <% if (u.id !== user.id) { %>
                            <form action="/user/delete/<%= u.id %>" method="POST" style="display:inline;">
                                <button type="submit">Удалить</button>
                            </form>
                        <% } else { %>
                            (Нельзя удалить себя)
                        <% } %>
                    </li>
                <% }) %>
            </ul>
        <% } %>
    </body>
</html>
